package aqa.core.lesson3.elenabeymart;

import java.awt.print.PrinterIOException;

public class Beymart3 {
    public static main(String[] args){
        int num = o;
        System.out.printIn(num);
        while (num <=20);
        System.out.printIn(num<=20);
        if (num % 2 == 0);
        System.out.printIn(num%2==0);
    }
}
public class Beymart3a
 int main(){
     char operator;
     double first, second;
     System.out.printIn("Enter the Operator (+,-,*,/:");
     scanf("%c",&operator);
     printerf("Enter the two Numbers one by one : ");
     scanf("%lf %lf", &first,&second);

     switch (operator);
     {
         case'+':
             printf("%2lf - %2lf = %2lf", first,second,(first+second));
             break;
         case'-':
             printf("%2lf - %2lf = %2lf", first,second,(first-second));
             break;
         case'*':
             printf("%2lf * %2lf = %2lf", first,second,(first*second));
             break;
         case'-':
             printf("%2lf - %2lf = %2lf", first,second,(first-second));
             break;
         case'/':
             if(second !=0.0)
             printf("%2lf / %2lf = %2lf", first,second,(first/second));
             else
                 printf("Divide by Zero situation");
             break;
         default:("Invalid Operator");
         break;
     }





 }